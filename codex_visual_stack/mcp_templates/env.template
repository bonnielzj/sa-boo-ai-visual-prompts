# Safe environment placeholder template
# Rename to .env locally; never commit real values.
OPENAI_API_KEY=${OPENAI_API_KEY}
GITHUB_TOKEN=${GITHUB_TOKEN}
FIGMA_ACCESS_TOKEN=${FIGMA_ACCESS_TOKEN}
AIMAMI_RELAY_URL=${AIMAMI_RELAY_URL}
AIMAMI_RELAY_TOKEN=${AIMAMI_RELAY_TOKEN}
CADQ_BIN=${CADQ_BIN}
PYTHON_BIN=${PYTHON_BIN}
AUTOCAD_MCP_SERVER_PATH=${AUTOCAD_MCP_SERVER_PATH}
SKETCHUP_MCP_BRIDGE_SCRIPT=${SKETCHUP_MCP_BRIDGE_SCRIPT}
SKETCHUP_PLUGIN_DIR=${SKETCHUP_PLUGIN_DIR}
ENSCAPE_INSTALLER_PATH=${ENSCAPE_INSTALLER_PATH}
